# HANDOFF-FOR-EXTERNAL-DEVELOPER-REVIEW — 2026-07-28 (v1, regenerated)

**Author:** Hermes session 2026-07-28 (initial handoff).
**Status:** REGENERATED 2026-07-31 by BBF-90.
**Source:** `docs/16_audit/HANDOFF-FOR-EXTERNAL-DEVELOPER-REVIEW-2026-07-28-v2.md` (the v2 handoff, which survived the BBF-86 F2 disaster). The v1 was the immediate predecessor; v2 incorporated the external reviewer's first-pass feedback.

**Note:** The v1 handoff is superseded by the v2 handoff. This
regenerated brief is a brief recap of what v1 contained; the v2
handoff is the canonical, surviving, full-length document.

---

## 0. The v1 handoff (recap)

The v1 handoff was the initial draft for the external reviewer's
first pass. It contained:

- Project overview (TL;DR, architecture summary)
- Test surface (unit + integration counts)
- The 6 most-recent BBFs in plain English (BBF-87, BBF-88.x, BBF-87.1, BBF-87.1.y, BBF-85, BBF-87.1.y follow-up)
- Problems encountered during the ship cluster (lichess rate-limit,
  4-of-7 archetype coverage, ALTER COLUMN DROP NOT NULL failure, etc.)
- Held-back queue (15 tickets ranked by impact)
- Honest disclosures (auto-derived corpora, no hand-curator path)
- Questions for the external reviewer (architecture, data integrity,
  schema, test infrastructure)

The v1 handoff was approximately 15 KB. The v2 handoff (which
survived) is 1064 lines / 47 KB and incorporates the first-pass
reviewer's feedback.

---

## 1. The reviewer's first-pass questions (recap)

The v1 handoff asked the reviewer 8 questions across 4 categories:

1. **Architecture:** Is the auto-derive approach for narrations
   acceptable given the no-hand-curator path?
2. **Data integrity:** Pre-BBF-87.1.y narrations rows in any
   production DB need backfill?
3. **Schema:** Should the FK to `positions(id)` be re-introduced?
4. **Test infrastructure:** Should the 7 integration skips be
   eliminated?

The reviewer answered these in v2, which led to:
- BBF-86.5 (kNN confidence gate)
- BBF-87.1.y (real FK)
- BBF-84B (deterministic fixture)

---

## 2. The v1 → v2 transition

The v1 → v2 transition was mechanical:

- §0-§5 (project overview, BBF descriptions, problems) — minor
  rewording only.
- §6 (held-back queue) — replaced with the v2 §13.3 Tier 1-4
  ranking after the reviewer's second pass.
- §7 (honest disclosures) — preserved.
- §8 (questions for reviewer) — preserved.
- §9-§14 (reviewer's first-pass + second-pass responses) — added
  in v2.

---

## 3. What v1 contained that v2 doesn't (or handled differently)

The v1 handoff had:

- A more verbose "Why this audit" section (T1-T3 framing) that v2
  condensed.
- An "Alternative review paths" section that v2 dropped.
- A "Known limitations" section that v2 split into "Honest disclosures"
  and "BBF-89."

The v2 handoff is the canonical reference; the v1 is a historical
artifact.

---

## 4. Regeneration metadata

- **Source material:** `docs/16_audit/HANDOFF-FOR-EXTERNAL-DEVELOPER-REVIEW-2026-07-28-v2.md` (full v2, 1064 lines).
- **Regenerated by:** BBF-90, 2026-07-31.
- **Confidence:** medium. The v1 was the predecessor of v2; the regen
  is a recap, not a byte-identical recreation.
- **Note:** The v2 handoff is the surviving reference. The regenerated
  v1 is a brief recap (15 KB → ~4 KB compressed). **The v2 handoff
  should be the primary reference for any future work.**
- **Human review recommended:** yes (per BBF-90 brief), but the v2
  is the source of truth.
