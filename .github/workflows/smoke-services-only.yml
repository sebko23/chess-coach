name: smoke-services-only

on:
  push:
    branches: [main]

jobs:
  hello:
    runs-on: ubuntu-latest
    services:
      redis:
        image: redis
        ports:
          - 6379:6379
    steps:
      - run: |
          echo "services block works"
          docker ps
