"""L-2 gold set and other labeled chess corpora.

Public API is exposed from submodules (e.g.
``chess_coach.datasets.l2_gold``). This package has no
runtime dependencies beyond the Python standard library;
the ``chess`` package is an optional import used only by
``L2GoldEntry.fen_parses()``.
"""
